.DS_Store
.npm/
.vscode
.idea
.public
node_modules
npm-debug.log
coverage
.cache
.envrc
tsconfig.tsbuildinfo
.env
!/.tool-versions
!/.envrc

# Story 03 Phase A: one-shot migration tooling. Scripts + vendored source
# snapshots + the flat-refdata oracle live here. Output is the committed
# refdata.json — the rest is intentionally not reproducible from the repo.
data-reconstruction/
